This is Perl module KeyedMutex::Memcached.

INSTALLATION

KeyedMutex::Memcached installation is straightforward. If your CPAN shell is set up,
you should just be able to do

    % cpan KeyedMutex::Memcached

Download it, unpack it, then build it as per the usual:

    % perl Makefile.PL
    % make && make test

Then install it:

    % make install

DOCUMENTATION

KeyedMutex::Memcached documentation is available as in POD. So you can do:

    % perldoc KeyedMutex::Memcached

to read the documentation online with your favorite pager.

Toru Yamaguchi
